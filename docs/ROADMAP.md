# News-Site Roadmap

Tracks planned improvements to the Situation Monitor. Each item graduates into the
feature pipeline (`docs/features/<slug>/`) when work starts; status here is the
single place to see where everything stands.

**Statuses:** `idea` → `planned` (pipeline docs in progress) → `in progress` (implementation) → `shipped`

_Last updated: 2026-09-12 (#17 shipped, PR #18; #18 specced through implementation plan; #19-#21 split out while speccing)_

## Now

| # | Item | Slug | Status | Notes |
|---|---|---|---|---|
| 1 | Test harness + CI (pytest, GitHub Actions) | `test-harness-ci` | shipped | `backend/tests/` (Postgres `newsdb_test`, respx-mocked upstreams, scheduler gated via `DISABLE_SCHEDULER=1`); 8 seed tests green. `.github/workflows/ci.yml` runs backend pytest + frontend build on PR/push. CI pins Python 3.11 (Railway runs 3.11.x). TC-7/TC-8 (red-check verification) to confirm on first PR |
| 2 | Adopt Alembic migrations (baseline + deploy hook) | `alembic-migrations` | shipped | Baseline `1cfc95e31a13` = prod (drift reconciled via Plan A into models); prod `stamp head` done 2026-07-07; Procfile runs `upgrade head`. `create_all` kept as release-1 safety net (remove in follow-up) |
| 6 | Fix district poll scraper + add VoteHub as a second district-polling source | `district-poll-scraper-fix` | shipped | PR [#8](https://github.com/samuellittlefield/News-Site-Aggregator/pull/8). v1: `house_polls.py` now fetches each state's consolidated page and resolves `District N → General election → Polling` via the section tree, keying tables off candidate-name `(R)`/`(D)`/`(I)` suffixes; primary-vs-general collision guarded (AC-2b), failures logged not silent (AC-4). v2: `votehub.py` queries `poll_type=us-representative` and upserts into `HousePoll` via a `Candidate`-table name→party crosswalk (never VoteHub's `partisan` field, AC-11; unmatched/ambiguous skipped+logged, AC-9). Additive `HousePoll.source` column (`wikipedia`\|`votehub`), migration `b2f1a7c4d9e3`. Tests `test_house_polls.py` + `test_votehub_house_polls.py` (TC-1…TC-11); full suite 22 passed |
| 4 | Per-source ingestion health tracking (`SourceRun` table → StatusPage) | `ingestion-health` | shipped | PR [#9](https://github.com/samuellittlefield/News-Site-Aggregator/pull/9). Upserted `SourceRun` row per registered scheduler job (status, last run/success time, item count, last error, cadence), recorded from all 17 `refresh_*` functions inside the existing try/except so failure isolation is unchanged, with recording itself defensively wrapped (AC-3). `refresh_faa` got a short-lived session; `refresh_breakout` excluded (not scheduled). `SOURCE_CADENCE` is the single source of truth shared by the scheduler and the new `GET /api/status/sources` route; TC-7 diffs registered job ids against it to catch drift. New "Data Sources" panel on StatusPage flags failing/stale sources + shows last error. Migration `c3d2e5f6a7b8`; tests `test_source_run.py` + `test_status_sources.py` (TC-1…TC-7), full suite 30 passed. Startup-refresh concurrency deferred as a non-goal/follow-up |
| 7 | Fix in-house forecast model's silent swing fallback | `forecast-model-swing-fallback-fix` | shipped | PR [#11](https://github.com/samuellittlefield/News-Site-Aggregator/pull/11). Bug found live 2026-07-21: House model showed 36% Dem vs. Kalshi's 83% Dem — `swing_d: 0` exactly, meaning `_current_env()` hit its hardcoded-2024-baseline fallback with zero live polling signal, silently. Added `GenericBallotAggregate` table, persisted from the Wikipedia aggregator rows `refresh_house_polls` already fetches every 6h (previously discarded), isolated in its own try/except (TC-9). `_current_env()` is now a 3-tier fallback (VoteHub live average → persisted aggregator mean → static 2024 baseline), returning a `swing_source` tag (`votehub`\|`aggregator`\|`fallback`) threaded through `run_model()`, `ChamberModel`, and the model card, which shows a warning only on the fallback tier (AC-5). `_kalshi_source`/`_polymarket_source` and `/api/polls/generic-ballot` untouched (AC-6, TC-10). Migration `d4e5f6a7b8c9`; tests `test_forecast_swing_source.py` + `test_generic_ballot_persistence.py` (TC-1…TC-5, TC-7…TC-10), full suite 54 passed |

## Next

| # | Item | Slug | Status | Notes |
|---|---|---|---|---|
| 3 | Auth on write endpoints (`/api/refresh`, candidate issue-tag POST/PUT) | `write-endpoint-auth` | shipped | PR [#10](https://github.com/samuellittlefield/News-Site-Aggregator/pull/10). `require_admin_key` (`auth.py`) gates the two candidate issue-tag routes via `X-Admin-Key` vs. `ADMIN_API_KEY` (read at call time, `secrets.compare_digest`, fails closed if unset); `/api/refresh` stays public (called from the public `TrendsPage` button) with a 60s in-memory cooldown instead (429 on repeats). Frontend `AdminPage`/`client.ts` prompt for the key once per session (memory only, never persisted), attach it to confirm/reject/add-tag calls, and re-prompt with a visible error banner on 401. Tests `test_write_endpoint_auth.py` (TC-1–TC-7, TC-10); full suite 43 passed. Browser-verified end to end |
| 5 | Frontend data layer: replace hand-rolled hooks with TanStack Query | `frontend-data-layer` | idea | `client.ts` is 987 lines of duplicated useState/useEffect. Likely folds into `extract-elections-app` ticket 3 rather than shipping separately |
| 9 | Extract elections/polling into a standalone project (gradual, multi-ticket) | `extract-elections-app` | planned | Feature plan `docs/features/extract-elections-app/01-feature-plan.md` (2026-07-23). Backend has no cross-domain imports (re-confirmed 2026-09-11: `routers/` and `services/` still flat, scheduler still 17 ungrouped `refresh_*` jobs, `client.ts` still 1,050 lines with no TanStack Query — **0 of 7 tickets implemented**). Re-sequenced 2026-09-11 against the 3 Nov election, split by whether a visitor can perceive the difference — see the Extraction tickets section below. Open questions narrowed from five to two for near-term work: naming, and what the Dashboard becomes on each side. Repo strategy, Astronomy's home, and shared-vs-duplicated health tracking all sit in the deferred half |
| 8 | Consolidate `/status` + `/admin` into one page | `status-admin-consolidation` | shipped | PR [#12](https://github.com/samuellittlefield/News-Site-Aggregator/pull/12). `AdminPage` is now the consolidated page: title + read-only intro, a summary banner reusing `SourceHealthSection`'s newly-exported `classify()`/`summarizeSourceHealth()` (AC-3, no divergent calculation), `SourceHealthSection` prominent near the top, `ServiceStatusSection` collapsed behind a native `<details>` by default (no new dependency). Default admin tab flipped `"pending"` → `"candidates"` so candidate browsing leads over tag review. Dead `POST /api/admin/run-tagger` empty-state line removed, replaced with accurate weekly-scheduled-job copy. `StatusPage.tsx` deleted; `/status` redirects client-side to `/admin` via `useNavigation.ts`'s `fromPath` (URL bar normalized via `history.replaceState`) — also caught and fixed one hardcoded internal link (`DashboardPage`'s `StatusPanel` → `navigate("status")`) that would've dangled otherwise. Frontend-only, no backend changes. All 12 cases in `04-test-cases.md` browser-verified manually (no Vitest harness yet); TC-10's network check confirmed only the 5 expected endpoints fire, no new/duplicate calls beyond pre-existing `React.StrictMode` dev-only doubling |

## Extraction tickets (item #9)

Sequenced 2026-09-11. The seven tickets divide by whether a visitor can tell the difference:
tickets 1-3 plus the frontend half of 5 are the entire perceptible product and never touch
the database; the rest deliver operational independence that is invisible from outside.

**Go/no-go: 2026-10-13.** If T1-T3 aren't merged by then, ship T5a's nav shell and domain on
the existing unsplit build instead. The failure mode to avoid is being half-migrated on
election night.

**Status 2026-09-11:** T1 shipped (PR #14) — see its row below. T2 has all four pipeline docs
written and approved (`docs/features/scheduler-split/`) and is ready for Claude Code, per T1's
explicit dependency (T2 changes a public endpoint's behaviour and needed the seam to exist
first). T3 and T5a are blocked on two decisions: what the polling product is called, and what
the Dashboard becomes on each side.

Grounding re-verified 2026-09-11 while speccing: still **zero elections↔monitor imports**
(the one candidate edge, three monitor services importing `services/topic_matcher.py`, is a
domain-free string matcher, not real coupling). The actual coupling is `models.py` — 25
classes in one file sharing one `Base` — which T1 splits while keeping that single `Base`.
Job domains split **8 elections / 8 monitor / 1 shared**, not the 5/12 implied by what
`/api/refresh` omits.

| Ticket | Item | Slug | Status | Target |
|---|---|---|---|---|
| T1 | Draw the backend seam — regroup `routers/` + `services/` into two bounded packages, no behaviour change | `elections-seam` | shipped | PR [#14](https://github.com/samuellittlefield/News-Site-Aggregator/pull/14). 47 modules regrouped into `app/elections/` (6 routers, 10 services), `app/monitor/` (6 routers, 21 services), `app/shared/` (1 router, 3 services) — exact match to the implementation plan's table. `models.py` split into a package (`elections.py`/`monitor.py`/`shared.py`), single `Base` in `app/models/base.py`, re-exported (with all 25 classes) from `__init__.py` so both `from app.models import X` and `alembic/env.py`'s `from app.models import Base` keep resolving. Verified locally before CI: one `Base`/one `MetaData` (25 tables), `alembic upgrade head` clean from empty, `autogenerate` empty diff — also wired that check into CI (closes the "autogenerate produces empty diff" chore, struck below). New `test_module_boundaries.py` AST-scans for cross-domain imports and was verified to actually fail on a deliberate violation, then reverted. Two things the docs didn't anticipate: three files resolved vendored CSVs via a path relative to their own file location, broken one directory level deeper (fixed, no data moved); and FastAPI's OpenAPI schema-name disambiguation folds in `__module__`, so 6 of 46 component names changed form when their router files moved (real JSON responses unaffected — verified via a pre-refactor baseline captured from a git worktree, `test_openapi_baseline.py`). No route/response/schema/frontend changes. Full suite 79 passed (59 pre-existing + 20 new); live-smoked one route per domain plus `/api/refresh` and the admin auth gate. T2 not started, per dependency |
| T2 | Split the scheduler into two independently-disableable job groups (still one process) | `scheduler-split` | **specced** | before election |
| T3 | Split `client.ts` into two domain modules + two nav shells, absorbing item #5's TanStack Query migration | `frontend-domain-split` | planned | before election |
| T5a | Second Vercel project + domain for the polling app, same API and database | `polling-front-door` | planned | before election |
| T4 | Second Postgres + fresh Alembic history, migrate elections tables and data | `elections-database` | idea | after election |
| T5b | Second backend deploy target for the extracted API | `elections-backend-deploy` | idea | after election |
| T6 | Resolve cross-cutting pieces: `SourceRun`/`ServiceStatus`, Astronomy, Dashboard on each side | `extraction-cross-cutting` | idea | after election |
| T7 | Physical repo split + CI/`CLAUDE.md`/`SOURCES.md` updates on both sides | `extraction-repo-split` | idea | after election |

## Polling data fixes (Sept 2026 audit)

Found 2026-09-11 auditing production ahead of the extraction. **Every one of these jobs
reports `status: success`** — none surfaced on the Data Sources panel. Freshness was measured
by poll fieldwork end date, not job execution time; job health only answers "did the scheduler
fire".

| # | Item | Slug | Status | Notes |
|---|---|---|---|---|
| 10 | Fix Economist/YouGov report discovery | `economist-discovery-fix` | shipped | PR [#13](https://github.com/samuellittlefield/News-Site-Aggregator/pull/13). Reproduced first: `fetch_pdf_links`'s regex/table parsing was never broken (unlike PR #8's district scraper) — Wikipedia's page itself had gone 46 days without a new citation (job-approval tables jump `July 2026` → `June 2026`, no August/September section; insource search for the missing report's filename returns zero hits), while YouGov kept publishing. Added VoteHub as a second discovery source (`_votehub_pdf_links`, mirrors PR #8's pattern) — it already ingests Economist/YouGov polls independently and stores each poll's source PDF url, recovering reports Wikipedia hasn't cited yet. `refresh_economist_yougov` now merges both sources; if both return zero candidates, it raises instead of returning a hollow success, so the scheduler records a `SourceRun` **failure** rather than `success`/`item_count: 0`. Verified live that merged discovery surfaces the previously-missed 2026-09-04/08 report. Tests `test_economist_yougov.py` (fixture-based parse regression, VoteHub filtering, incident-reproduction, zero-candidates-is-a-failure at both the service and scheduler/SourceRun level); full suite 59 passed |
| 11 | Staleness labelling on poll cards | `poll-staleness-labels` | shipped | PR [#15](https://github.com/samuellittlefield/News-Site-Aggregator/pull/15). Deliberately narrow: no change to weighting, window length, or any returned average value — `compute_average` (`votehub.py`) gains additive `newest_fieldwork_end`/`oldest_fieldwork_end` fields only. Hard gate (AC-3) verified by diffing a pre-change fixture: `swing_d`, `swing_source`, `dem_prob`, `rep_prob`, `median_dem_seats` identical for both chambers, `test_staleness_regression.py` pins it permanently. Shared frontend helper (`lib/pollStaleness.ts`, `classify()`-in-`SourceHealthSection` precedent) adds a stale marker (10-day threshold, icon+text not color alone) and a promoted thin-average badge (under 3 polls) to `VoteHubApprovalCard`, the Dashboard `PollsPanel`'s inline VoteHub blocks, and a stale marker on `ApprovalSection`; `VoteHubApprovalCard`'s silent `if (!avg) return null` replaced with an explicit state naming the last known fieldwork date. One deviation from the implementation plan documented in the PR: retargeted from `GenericBallotBar.tsx` (which renders the explicitly-out-of-scope VoteHub+aggregator merged `/api/polls/generic-ballot`) to `PollsPanel.tsx`'s inline block (the actual `compute_average` generic-ballot consumer). Live-verified against real data (approval genuinely frozen at fieldwork 2026-08-27/28, confirming the ticket's premise) with full backup/restore; caught and fixed a bug where the Dashboard badge showed only one of stale/thin when both applied. `SOURCES.md` documents AC-10 (generic-ballot drain → forecast tier-2 fallback trigger). Full suite 87 passed (79 + 8 new) |
| 12 | District poll data quality cleanup | `district-poll-data-quality` | idea | Wikipedia stream (`house_polls.py`): 3 rows dated in the future (end_date 2026-09-28, seen 09-11); 82 of 112 rows null on `start_date`/`source_url`/`sample_size` — **corrected 2026-09-12: this is not a parse failure.** `_housepoll_from_extract` (`house_polls.py:632-646`) simply never passes those three columns, so the Wikipedia path structurally cannot populate them; no parser bug to hunt. Also note `_extract_polls_from_polling_section` defaults `end_date` to `now` when a date won't parse, which dates bad rows as fresh; markup bleeding into the pollster field (`Emerson Collegename=NPI`, truncated footnote text ending mid-word); pollster-name variants defeating dedup (`SurveyUSA` vs `Survey USA` — 9 duplicate groups / 20 rows). Coverage 37 of 435 districts. All cosmetically visible on the district map and carousel |
| 13 | `SourceRun` quiet-source detection (backend only) | `sourcerun-staleness` | specced | Specced 2026-09-12, `docs/features/sourcerun-staleness/`. **Rescoped 2026-09-12 to backend only.** Originally included a fifth `classify()` state and a new badge on the Admin panel; dropped after realising (a) the panel is a one-reader surface, (b) it is currently public (see #16), and (c) the 46-day Economist failure was a nobody-was-looking failure, not a rendering failure — a nicer badge would not have caught it. Now: `last_nonempty_at` on `SourceRun` (set only when `item_count > 0`), a declared `expected_data_interval_minutes` per source (`None` = exempt, e.g. `issue_tagger_job`), and a **server-computed `data_state`** on the route so every reader gets the same verdict. No frontend change. Alerting deliberately deferred — zero traffic today makes it heavy-handed |
| 14 | Decay-weighted VoteHub averaging (replace the 21-day hard cutoff) | `votehub-decay-weighting` | idea | Follow-up to #11 (`poll-staleness-labels`), split out deliberately — this one **does** move forecasts, so it needs its own review and no deadline pressure. `compute_average`'s hard 21-day window + `sqrt(sample_size)` weighting is cruder than what VoteHub does with the same raw polls: they publish a methodology of smooth recency decay (rate varying with polling volume so sparse questions fade older polls *more slowly*), pollster-quality/house-effect adjustment, and down-weighting a pollster that floods a race. Removing the cutoff entirely (rather than labelling it) would end the #11 staleness-marker need at the source. Changes `_current_env()`'s tier-1 swing input, so chamber forecasts move — must be measured and reviewed deliberately, never bundled with a labelling fix again. Worth deciding: re-derive our own decay average, or ingest VoteHub's own published average and keep local computation as a documented fallback (counter-argument: deepens dependence on a feed currently 15+ days stale). **Also carries a gap #11 left open (decided 2026-09-12):** #11 could not label `GenericBallotBar` because it is presentational and fed from the merged `/api/polls/generic-ballot` endpoint, which #11 put out of scope — so only `PollsPanel`'s inline VoteHub block got the treatment and the generic ballot's own card on the Polls page still has no staleness label. It has the same drain exposure (healthy at 4 days today only because that stream still delivers). This ticket touches the merged endpoint anyway, so the labelling gap gets closed here rather than as its own row |
| 15 | Kalshi fetch failure reported as success, and empty-fetch retirement | `kalshi-fetch-failure` | shipped | PR [#16](https://github.com/samuellittlefield/News-Site-Aggregator/pull/16). Fragility fix, not a live-outage response — Kalshi recovered on its own within the hour of the 2026-09-12 incident, so reproduced with a mocked 403 rather than chasing the connectivity issue. Two defects fixed: (1) `fetch_kalshi` now tracks per-series outcomes in `series_errors`; if *every* series fails (request error, non-2xx, or an unparseable/unexpected body) it raises, so the scheduler wrapper's existing `except` records a real `SourceRun` **failure** with the upstream error, instead of a clean return read as `success`/`item_count: 0`. A partial failure (one series up, one down) still saves what it got and returns normally. (2) The retirement query's `notin_(seen_ids)` against an empty set excluded nothing and flipped every stored Kalshi market inactive on any run that found nothing — guarded on non-empty `seen_ids`, plus scoped to only the series actually fetched this run (market ids are always `"<SERIES_TICKER>-..."`), since on a partial failure `seen_ids` only contains the healthy series' tickers and an unscoped guard would still retire the *down* series' existing markets. A real disappearance within a successfully-fetched series still retires normally (TC-4, unbroken). Deliberately untouched: `MIN_VOLUME_24H` and the series tickers (live volume was 163k-178k, well over the 1000 floor), the in-house forecast model, and the connection itself (no header/host change — `api.elections.kalshi.com` returns `yes_bid`/`last_price` as null where `external-api` returns populated `*_dollars` fields, not a drop-in). Extended `test_kalshi_upsert.py` — it existed and passed before this, which is why CI didn't catch the incident; full suite 94 passed (87 + 7 new) |
| 16 | Gate the ingestion-health surface (`/api/status/sources` + `/admin`) | `status-endpoint-auth` | shipped | PR [#17](https://github.com/samuellittlefield/News-Site-Aggregator/pull/17). `require_admin_key` added to `GET /api/status/sources` (single consumer verified — `AdminPage`'s `useSourceRuns`); `GET /api/status` deliberately left public — third-party statuspage data consumed by the public Dashboard's `StatusPanel`/`ServiceStatusSection`, gating it would break the public dashboard for no benefit. Frontend: the admin key is now prompted on `/admin` page load rather than first write — `useSourceRuns` dropped its internal self-polling in favor of a `refresh()` the page drives through the existing `runAdminAction` gate (on mount + the same 5-minute interval), so a missing or wrong key re-prompts exactly like the existing write flow (401 → "Admin key was rejected"). Live-verified all three paths (prompt-on-load, wrong-key rejection, correct-key success) against a temporary local key, restored after. Tests: 4 new cases in `test_status_sources.py` (401 no key / wrong key / key unset, `/api/status` stays public); regenerated the elections-seam `openapi_baseline.json` fixture for the route's new `X-Admin-Key` parameter, diffed first to confirm no other drift. Full suite 98 passed |
| 17 | District poll coverage driven by Wikipedia, not a hardcoded list | `district-coverage-from-wikipedia` | shipped | PR [#18](https://github.com/samuellittlefield/News-Site-Aggregator/pull/18). Diagnosed 2026-09-12: the flat 112 rows / 37 districts was **not** an ingestion failure — both jobs ran and succeeded, and `item_count: 0` correctly meant "no new `poll_id`" (both counters increment only on insert). `fetch_district_polls` built its work list from `CompetitiveDistrict`, which only ever held the 60 rows of the hardcoded `COMPETITIVE_DISTRICTS` literal, unchanged since `a39fc85` (2026-06-02) — a 13.8% ceiling. Measured live across all 50 state pages: **86 districts had a `District N → General election → Polling` section; the scraper visited 31; 55 were never fetched** (IA-1, IA-2, CO-3, NE-1, PA-10, TX-23, VA-1, NY-21, MI-4, CA-40 among them), while **29 of the 60 seeded districts had no polling section at all** (half the seed list was dead fetches every 6h). Fix: a new `_iter_polling_sections` enumerates every district straight from each state's section tree instead, so coverage grows on its own; `_find_polling_section` becomes a thin wrapper over it, keeping the pre-existing section-resolution tests passing unmodified as the refactor's regression gate. `CompetitiveDistrict` keeps serving Cook ratings and centroids on `/api/polls/house/districts`, but stops being a scrape allowlist. **At-large states folded in**: AK, DE, ND, SD, VT, WY 404'd on every run — they use the *singular* `...election_in_<State>` title; they have no `District N` wrapper (`General election` at toclevel 1), so a top-level `General election → Polling` resolves and stores as district `0` per `polls.py:30-32`. Zero districts resolving a polling section across all 50 states now raises, so the scheduler records a `SourceRun` **failure** instead of a hollow success — same pattern as #10/#15; a partial run still succeeds. No model change, no migration, no frontend change. `04-test-cases.md` written from the approved AC-1 through AC-12 before implementation; 16 new tests plus the 8 pre-existing, full suite 114 passed. `SOURCES.md`'s "House district polls" row updated |
| 18 | VoteHub district-poll candidate crosswalk fix | `votehub-candidate-crosswalk-fix` | shipped | PR [#19](https://github.com/samuellittlefield/News-Site-Aggregator/pull/19). Second, independent cause of the flat 112. Of 92 `us-representative` polls VoteHub returns, 8 have a null `seat_name` and **54 of the remaining 84 were dropped by the crosswalk — only 30 stored, across 17 districts** (a re-implementation run against live FEC data reproduced the 30 exactly). Root cause: `_normalize_name` sorted tokens and demanded **exact set equality**, so FEC legal names (`Miller-Meeks, Mariannette Jane`) never matched VoteHub ballot names (`Mariannette Miller-Meeks`). Causes: 20 nickname-vs-legal given name (`Hurd, Jeffrey` / `Jeff Hurd`), 16 FEC extra token, 9 generic `"Rep"`/`"Dem"` answer labels, 7 AK-01 at-large key mismatch (`_parse_seat` mapped `AK-AL`→0 but VoteHub sends `AK-01`→1 while FEC uses 0), 4 diacritics (`María Elvira Salazar`), 4 absent from FEC. Fix: `_resolve_candidate` keys the crosswalk on a structural comparison — FEC surname tokens as a contiguous **suffix** of the VoteHub name's tokens (never the last token — that fails every multi-token surname: `De La Cruz`, `Van Orden`, `Gluesenkamp Perez`, `Miller-Meeks`, `von Wilpert`, 11 cases, the largest single bucket) plus first-given-name agreement, diacritics folded via Unicode NFD, falling back to the surname alone only when unique within the district's **stored** candidates; **AC-5 (never guess on ambiguity) and AC-6 (never trust `partisan`) both preserved and covered by dedicated tests, including a static source-guard**. At-large seats (`AK`, `DE`, `ND`, `SD`, `VT`, `WY`) now resolve to district `0` regardless of `-01` vs `-AL`. Generic-label polls (`"Rep"`/`"Dem"`) get their own skip cause, distinct from a name-matching failure — decided 2026-09-12, out of scope for v1 since they're generic-ballot-shaped questions, not named head-to-heads. Adds one `logger.info` aggregate summary per run (returned/bad_seat/too_few_answers/inserted/updated/skipped-by-cause) alongside the existing per-poll WARNINGs — nothing counted the skips before, so a 64% drop rate read as a healthy run. New committed fixture `tests/fixtures/fec_house_filed_2026.json` backs an AC-4b regression tripwire: no district polled has a surname unique among stored candidates but ambiguous among all FEC-filed candidates — a future failure is the signal to promote #19, not to loosen AC-4. **Not independently re-verified against the live VoteHub/FEC feeds during implementation** (no live network calls made this session) — the plan's live-measured "30 stored → 67 (+37)" recovery count is the plan author's own 2026-09-12 run; what shipped was verified to implement the specified algorithm exactly, via fixture-based tests covering every documented failure bucket (AC-1 through AC-13). No model change, no migration, no frontend change; `fetch_votehub_polls` and `compute_average` untouched, #11's `test_staleness_regression.py` pin still passes. `04-test-cases.md` written from the approved AC-1 through AC-13 before implementation; full suite 137 passed (98 + 39 new/extended). `SOURCES.md`'s House-district-polls row updated with the new matching rule **Live-verified 2026-09-13 in Cowork** (the caveat PR #19 flagged is now closed): the merged matcher's pure functions were extracted from `votehub.py` by AST and run against the live VoteHub feed and live FEC data — **67 stored, exactly as predicted** (returned 92, bad_seat 8, generic_label 9, unresolved 8, ambiguous 0). The residual 8 match the spec district for district: AK-AL x3 + CA-06 + CA-40 (non-D-vs-R generals, #20), `Nick LaLota` NY-01, `Janelle Stetson` PA-10, `Lupe Castillo` IL-04 (#19). The VoteHub feed was byte-identical to the 09-12 capture (same 92 poll ids), so the before/after is a clean comparison. |
| 19 | Store the full FEC filed-candidate set behind a display-eligibility flag | `fec-filed-candidate-scope` | idea | Split out of #18 on 2026-09-12 rather than widening that ticket before the election. `fetch_house_candidates` filters on `incumbent == "I" or (state, district) in competitive_keys or has_raised_funds` (`fec_candidates.py:217`), which excluded **1,011 of 3,648** filed House candidates as of 2026-09-12. Two consequences: (a) #18's AC-4 surname-uniqueness fallback asserts uniqueness against a *subset*, so a surname can be unique among stored rows while colliding with a filtered-out filer — measured at **zero occurrences today** across all 48 polled districts / 161 candidate-name answers, and pinned by #18's AC-4b detector test, which failing is the signal to promote this row; (b) three VoteHub polls are droppable only because their candidate was filtered out (`Brennan Barrington` OH-15, `Lupe Castillo` IL-04, `Chris Schmidt` NY-21) — they are filed with FEC, not absent from it. Cannot be done by simply loosening the filter: `DistrictOut.candidates` (`polls.py`) feeds the district map's candidate lists, so 1,011 no-money filers would surface there, and the weekly `issue_tagger_job` would widen its scope too. Needs a display-eligibility column set from the existing filter predicate, the two display consumers reading it, and an Alembic migration. Also folds in the standing chore to add `FEC_API_KEY` to `backend/.env.example` |
| 20 | District polls with no Democrat-versus-Republican pairing | `house-poll-nondr-generals` | idea | Found 2026-09-12 while speccing #18. Alaska's ranked-choice and California's top-two primaries can produce a general election with no D-vs-R matchup at all: measured live, AK-AL (REP vs IND, two polls), CA-06 (DEM vs OTH), CA-40 (REP vs REP). `HousePoll` has only `dem` and `rep` columns, so these 4 polls resolve their candidates correctly and are then discarded for want of a D/R pair — invisible today, and it is a **schema** limitation, not a matching bug, so #18 deliberately does not touch it. Note `_extract_polls_from_polling_section` already parses an `(I)` column into `ind_val` and then drops it for the same reason, so the Wikipedia stream has the identical gap. Any fix needs a way to represent an arbitrary candidate/party pair per poll (a child table, or a JSONB answers column mirroring `VoteHubPoll.answers`) plus a decision about how the district map and carousel render a same-party race — so it is a model change, a migration, and a frontend question, not a small one. Worth weighing against how much of the map is CA and AK |
| 21 | District-poll empty state asserts an upstream cause it can't know | `district-empty-state-honesty` | idea | Found 2026-09-12 while reviewing what #17 means for a user. `PollCarousel.tsx:88` already degrades correctly — with zero district polls it renders the national generic-ballot aggregators instead of a blank panel — but its copy reads "No district polls **yet** — showing national generic ballot aggregators. Individual polls will appear here as they're **published**," which asserts that *Wikipedia has nothing*. That is the one explanation guaranteed to be false if our own ingestion breaks: the same benign-cause-hard-coded pattern as #10 (46-day Economist silence) and #15 (Kalshi), this time in frontend copy rather than a `SourceRun` status. **#17 sharpens it** — once coverage is driven by what Wikipedia actually has (86 districts with polling sections as of 2026-09-12), an empty carousel really would mean something on our side broke. Not urgent: the carousel has never been empty (112 rows today) and #17 widens coverage further, so the state does not currently occur. Scope is one string plus a decision about whether the panel should distinguish "no polling exists" from "we could not fetch polling" — the latter needs the component to see ingestion health, which is admin-gated since #16, so probably phrase it neutrally rather than wire up a new data dependency. Deliberately kept out of #17 to preserve that ticket's no-frontend-change property. Sits naturally alongside #12's district-surface cosmetics |

## Later / chores

| Item | Status | Notes |
|---|---|---|
| Python version cleanup: rebuild local venv on 3.11, drop 3.9 syntax constraint, update pipeline skill | idea (overdue) | Resolved 2026-07-02: Railway runs 3.11.x; CI pins 3.11. Local venv still 3.9 until rebuilt |
| Add `FEC_API_KEY` to `backend/.env.example` | idea | Key exists in `.env` but not the example — violates repo convention |
| Add a README | idea | No README at repo root |
| Update `news-site-feature-pipeline` skill — conventions are stale | **reopened 2026-09-12** | Skill still says "no test harness yet" (pytest + CI shipped, PR #7) and "backend runs 3.9" (Railway + CI are 3.11), and has no Alembic migration checklist. Update via Settings > Capabilities. **Reopened 2026-09-12:** the skill still claims `SOURCES.md` "carries a stale 'Python runtime is 3.9' line" — it does not, and has not for some time: lines 12-13 already read "Railway and CI run Python 3.11; Samuel's local venv is still 3.9 (rebuild pending) — keep `Optional[...]`", which is exactly right. That false claim propagated into #17's Claude Code kickoff as an instruction to "fix" a correct line; Code checked, found nothing stale, and refused rather than writing something false. Also still stale: the backend-layout conventions and the `03-implementation-plan` template reference flat `backend/app/services/` and `backend/app/models.py`, which the elections-seam split (T1, PR #14) replaced with the `elections/`/`monitor/`/`shared/` packages and `app/models/` |
| Vitest / frontend unit tests | idea | Deferred from `test-harness-ci` non-goals |
| `ModelForecast` ingest for Split Ticket (per `SOURCES.md` forecasting sweep) | idea | Blocked on confirming a stable data endpoint |
| Ballot measures / propositions as a data source | idea | Raised 2026-09-11. Coverage of what people actually vote on beyond candidates — CA propositions and other states' ballot measures — framed by *impact* rather than horse race. **Bearing on naming:** this would make the extracted product broader than "polling," so the T5a domain decision should wait until this direction is settled. Placeholder display name until then: "Elections" (already the internal package name). No upstream source identified yet |

## Parked

| Item | Slug | Status | Notes |
|---|---|---|---|
| Hacker News trending source | `hacker-news-trending` | draft (dry run) | Full pipeline docs exist; never implemented |

## Shipped

| Item | Slug | Shipped |
|---|---|---|
| Polling page liquid-glass retheme | `polling-page-glass-theme` | PR #4, `26f93ce` (2026-07-02) |
